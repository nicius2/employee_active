import styled from "styled-components";

export const LayoutDivStyle = styled.div`
     display: flex;
     align-items: start;
     width: auto;
     padding: 15px 20px 0 20px;
     overflow: hidden;
`

export const LayoutNavOutlet = styled.div`
     display: flex;
     flex: 1;
     gap: 1.5rem;
     flex-direction: column;  
`